name: iondtn-docker
services:
  ion-dtn:
    build: .
    ipc: host
    container_name: ion-dtn
    hostname: ion-dtn
    privileged: true
    volumes:
    - ./signature_replay:/ION-DTN/tests/signature_replay