version: "3.8"

services:
  node1:
    build: .
    container_name: node1
    hostname: node1
    networks:
      - ion-net
    stdin_open: true
    tty: true

  node2:
    build: .
    container_name: node2
    hostname: node2
    networks:
      - ion-net
    stdin_open: true
    tty: true

networks:
  ion-net:
    driver: bridge