name: iondtn-docker
services:
  ion-dtn:
    build: .
    shm_size: "1g"
    container_name: ion-dtn
    hostname: ion-dtn
    privileged: true
    volumes:
    - ./signature_replay:/ION-DTN/tests/signature_replay